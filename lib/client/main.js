// Generated by CoffeeScript 1.6.3
require(['three', 'realm/ui/user_interface', '/js/realm/engines/classical/classical_mechanics.js', '/js/actors/boundary_sphere.js', 'realm/visualizer/web_g_l_visualizer', '/js/realm/realm.js'], function(three, UserInterface, ClassicalMechanics, BoundarySphere, WebGLVisualizer, Realm) {
  var exist, globals, realm, renderer, simulator, ui;
  globals = {
    document: document,
    window: window,
    THREE: THREE
  };
  ui = new UserInterface(globals);
  simulator = new ClassicalMechanics(globals, ui, {
    time: 1,
    paused: false
  });
  renderer = new WebGLVisualizer(globals, ui, {
    clearColor: 0x444444,
    clearAlpha: 1,
    fov: 120
  });
  realm = new Realm(globals, ui, {
    engine: simulator,
    visualizer: renderer
  });
  realm.registerActor(new BoundrySphere(globals, ui, {
    radius: 500,
    segments: 200,
    rings: 200
  }));
  exist = function() {
    requestAnimationFrame(exist);
    return realm.exist();
  };
  return exist();
});
